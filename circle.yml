machine:
  python:
    version: 2.7.10
    version: 3.5.1

dependencies:
  override:
    - make update-all
  post:
    - python --version
    - virtualenv --version
    - pip --version
    - uname -a
    - lsb_release -a

test:
  override:
    - make ci
