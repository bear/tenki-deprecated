machine:
  services:
    - redis
    - docker
  python:
    version: 2.7.10

dependencies:
  override:
    - curl -L https://github.com/docker/compose/releases/download/1.5.0/docker-compose-`uname -s`-`uname -m` > ../bin/docker-compose && chmod +x ../bin/docker-compose
    - make update-all

test:
  override:
    - DOCKER_IP=`docker-machine ip` make ci
